#include <stdio.h>
#include <stdlib.h>
//if it's with publish library, use name in <>
//if it's your own work, use ""
//NO semicolon! It complies this first, so don't put it because it's wrong!

int main (){
  //no methods in C, only functions
  //CAN put command line argument (String[]args) but doesn't have to
  //NOT object oriented so don't need that public static void

  printf("Hello World");
  //f is for Formatted -> for contactinations and w.e
  
  return 0;
  //return type int (standard); may use void but it wouldn't yell at you if you're wrong
}
